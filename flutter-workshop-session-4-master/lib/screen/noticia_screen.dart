import 'package:flutter/material.dart';

class NoticiaScreen extends StatelessWidget {
  static const routeName = '/noticia';
  const NoticiaScreen({Key key}) : super(key: key);
 
  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(

      ),
    );
  }
}
